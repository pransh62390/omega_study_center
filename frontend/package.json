{
  "name": "question_bank",
  "version": "1.0.0",
  "description": "",
  "keywords": [],
  "license": "ISC",
  "author": "Pransh Gupta",
  "type": "commonjs",
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html --public-url /omega_study_center/ --dist-dir dist --no-source-maps",
    "pages": "npm run build && rm -rf ../docs && mkdir -p ../docs && cp dist/index.html dist/*.js dist/*.css ../docs/ && cp public/404.html ../docs/404.html && cp public/.nojekyll ../docs/.nojekyll",
    "deploy": "npm run pages && gh-pages -d dist",
    "clean": "node clean.js",
    "test": "jest"
  },
  "devDependencies": {
    "buffer": "^6.0.3",
    "gh-pages": "^6.3.0",
    "parcel": "^2.16.4",
    "process": "^0.11.10"
  },
  "dependencies": {
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "react-pdf": "^10.5.0",
    "react-router-dom": "^6.28.0"
  },
  "browserslist": [
    "last 2 chrome version",
    "last 2 firefox version",
    "last 2 versions"
  ]
}
